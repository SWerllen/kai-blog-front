import { ArticleDetailMessage } from './article-detail-message';

describe('ArticleDetailMessage', () => {
  it('should create an instance', () => {
    expect(new ArticleDetailMessage()).toBeTruthy();
  });
});
